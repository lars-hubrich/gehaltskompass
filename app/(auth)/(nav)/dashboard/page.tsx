"use client";

import MainGrid from "@/components/dashboard/MainGrid";

/**
 * Renders the dashboard landing page containing the main grid.
 *
 * @returns {JSX.Element} The dashboard page component.
 */
export default function DashboardPage() {
  return <MainGrid />;
}
